# WARNING: Example values for local development only.
# Generate strong, unique secrets (tokens + passwords) for production, e.g.:
#   openssl rand -hex 32    # admin/readonly API tokens
#   python - <<'PY'         # strong password suggestion
#   import secrets; print(secrets.token_urlsafe(24))
#   PY
RECOZIK_ACOUSTID_API_KEY=demo-key
RECOZIK_ADMIN_TOKEN=dev-admin
RECOZIK_AUDD_TOKEN=
RECOZIK_WEB_ADMIN_USERNAME=admin
RECOZIK_WEB_ADMIN_PASSWORD=dev-password
RECOZIK_WEB_READONLY_TOKEN=
RECOZIK_WEB_PRODUCTION_MODE=false

# Optional overrides for docker compose (defaults: /data + uploads)
RECOZIK_WEB_BASE_MEDIA_ROOT=/data
RECOZIK_WEB_UPLOAD_SUBDIR=uploads

# Optional: override dashboard upload limit (passed at build time)
RECOZIK_WEBUI_UPLOAD_LIMIT=100mb
